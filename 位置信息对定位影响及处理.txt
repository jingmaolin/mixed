问题：
手机位置信息开启与否对定位的影响：
小米手机位置服务关闭仍可定位，
但华为手机位置服务关闭后不可定位

1.手机的位置信息
2.应用的定位权限
3.高德处有返回码进行判定

解决办法：
根据高德的返回码来判断是否缺少定位权限，
如果缺少则提示“应用定位权限或手机位置信息未开启”
或缺少时首先判断应用的定位权限是否开启（未开启则提示，结束），
其次判断应用的位置服务是否开启（未开启则提示，结束）

定位权限的 framework底层代码